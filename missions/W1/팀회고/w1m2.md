# SQL Tutorial 토의

파이썬에서 SQL을 실행하기 위해 sqlite3를 이용한 데이터베이스 연결과 테이블 생성 과정을 먼저 이해해야 했다. 또한 SQL Tutorial의 일부 문법(ANY, ALL, SELECT INTO, Stored Procedure 등)은 SQLite에서 지원되지 않아 다른 SQL 문법으로 대체하여 실습을 진행했다. 이러한 차이를 확인하면서 DBMS마다 지원하는 SQL 문법에 차이가 있다는 점을 이해할 수 있었다.